# tl;dr
```./build.sh``` to build the image.
```./run.sh``` to run it the container.

# Notes
This repo sets up vscode in a container. It also installs basic python and g++ compiler to run C++ code. 
It also adds an user (this needs some clean-up from other examples). It also requires ```code``` and ```data``` directories.